- if @account.errors.any?
  #error_explanation
    %h2
      = pluralize(@account.errors.count, "error")
      prohibited this account from being saved:
    %ul
      - @account.errors.full_messages.each do |msg|
        %li= msg
.form-group
  = form.label :account_type
  = form.text_field :account_type, class: 'form-control'
.form-group
  = form.label :branch
  = form.text_field :branch, class: 'form-control'
.form-group
  = form.label :minor_indicator
  = form.check_box :minor_indicator, class: 'form-control'
